fun main() -> i64 {
    // a b c
    let sum = 0;
    for(let i = 0; i < 5; i = i + 1) {
        sum = sum + fib(i);
    }
    return sum;
}

fun fib(n : i64) -> i64 {
    if (n == 0) {
        return 0;
    }

    if (n == 1) {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}