main();

fun main() {
    // a */ b c
    let a: i32 = 5;
    const b: i32 = 5;
    if (5) {
        return;
    }

    let res: i32 = test(a, b);
    return;
}

fun test(a: i32, b: i32): i32 {
    return a + b;
}