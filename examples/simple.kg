fun fib(n : i64): i64 {
    if (n == 0) {
        return 0;
    }

    if (n == 1) {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}

fun main() {
    // a b c
    for (let i = 0; i < 30; i = i + 1) {
        let res = fib(i);
        print(res + "\n");
    }
    return;
}

main();