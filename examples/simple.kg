fun main() -> i64 {
    // a b c
    const i = 30;
    return fib(i);
}

fun fib(n : i64) -> i64 {
    if (n == 0) {
        return 0;
    }

    if (n == 1) {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}