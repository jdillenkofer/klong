main();

fun main() {
    // a b c
    const i: i64 = 2;
    let res = fib(i);
    print(res);
    return;
}

fun fib(n : i64): i64 {
    if (n == 0) {
        return 0;
    }

    if (n == 1) {
        return 1;
    }
    return fib(n - 1) + fib(n - 2);
}