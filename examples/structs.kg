pub fun main() -> i64 {
    let node: Node;
    let i = sizeof(Node);
    // node.value = 0;
    // node.next = cast<ptr<Node>>(0);
    return cast<i64>(i);
}

struct Node {
    value: i64,
    next: ptr<Node>
}