pub fun main() -> i64 {
    let node: Node;
    let i = sizeof(Node);
    node.value = 0;
    node.next = cast<ptr<Node>>(0);
    return node.value;
}

struct Node {
    value: i64,
    next: ptr<Node>
}